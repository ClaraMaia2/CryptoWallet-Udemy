<!DOCTYPE html>
<html>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js" integrity="sha384-YvpcrYf0tY3lHB60NNkmXc5s9fDVZLESaAA55NDzOxhy9GkcIdslK1eN7N6jIeHz" crossorigin="anonymous"></script>

  <p style="color: green"><%= notice %></p> 

  <h1><%= MiningType.model_name.human(count: @mining_types.count)%></h1>

  <table class="table table-hover">
    <thead>
      <tr>
        <th><%= @mining_types.human_attribute_name(:description)%></th>
        <th><%= @mining_types.human_attribute_name(:acronym)%></th>
        <th colspan="3"></th>
      </tr>
    </thead>
    <tbody>
      <% @mining_types.each do |mining_type| %>
        <tr>
          <td><%= mining_type.description %></td>
          <td><%= mining_type.acronym %></td>
          <td>
            <%= link_to t('links.show'), mining_type %>
          </td>
          <td>
            <%= link_to t('links.edit'), mining_type %>
          </td>
          <td>
            <%= link_to t('links.delete'), mining_type, method: :delete, data: {confirm: t("messages.confirm")} %>
          </td>
        </tr>
      <% end %>
    </tbody>
  </table>

  <%= link_to t('new.mining_type'), new_mining_type_path, class: "btn btn-primary" %>
</html>
